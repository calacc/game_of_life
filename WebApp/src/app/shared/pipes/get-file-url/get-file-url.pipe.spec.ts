import { GetFileUrlPipe } from './get-file-url.pipe'

describe('GetFileUrlPipe', () => {
    it('create an instance', () => {
        const pipe = new GetFileUrlPipe()
        expect(pipe).toBeTruthy()
    })
})
