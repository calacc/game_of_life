import { FormAutoFocusDirective } from './form-auto-focus.directive'

describe('FormAutoFocusDirective', () => {
    it('should create an instance', () => {
        const directive = new FormAutoFocusDirective()
        expect(directive).toBeTruthy()
    })
})
