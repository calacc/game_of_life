import { ModalFormAutoFocusDirective } from './modal-form-auto-focus.directive'

describe('ModalFormAutoFocusDirective', () => {
    it('should create an instance', () => {
        const directive = new ModalFormAutoFocusDirective()
        expect(directive).toBeTruthy()
    })
})
